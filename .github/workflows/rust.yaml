name: Rust

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    name: Check formatting, lint, build and tests! 🚀

    steps:
    - uses: actions/checkout@v4

    - name: Cache dependencies
      uses: actions/cache@v4
      with:
        path: |
          ~/.cargo/registry
          ~/.cargo/git
          target
          lcov.info
        key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}

    # - name: Check formatting
    #   run: cargo fmt --all -- --check

    # - name: Lint with clippy
    #   run: cargo clippy --all-targets --all-features -- -D warnings

    - name: Build
      run: cargo build --verbose

    # - name: Run tests
    #   run: cargo test --verbose

    # - name: Install cargo-llvm-cov
    #   uses: taiki-e/install-action@cargo-llvm-cov

    # - name: Generate code coverage
    #   run: cargo llvm-cov --ignore-filename-regex "infrastructure|main.rs" --all-features --workspace --lcov --output-path lcov.info

    # - name: Upload coverage to Codecov
    #   uses: codecov/codecov-action@v3
    #   with:
    #     token: ${{ secrets.CODECOV_TOKEN }}
    #     files: lcov.info
    #     fail_ci_if_error: true

    - name: Upload binary to EC2
      uses: appleboy/scp-action@v0.1.4
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        source: "target/debug/prex_core_challenge"
        target: "/home/${{ secrets.EC2_USER }}/prex_core_challenge"
        strip_components: 2
        overwrite: true

    - name: Restart app on EC2
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          chmod +x /home/${{ secrets.EC2_USER }}/prex_core_challenge
          pkill prex_core_challenge || true
          nohup /home/${{ secrets.EC2_USER }}/prex_core_challenge > app.log 2>&1 &